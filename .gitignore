# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment — real secrets NEVER go here. Server-side secrets (service role
# key, API keys, X credentials) live in the Supabase dashboard. Only public
# VITE_ build vars may sit in the tracked .env.
.env.local
.env.*.local
.env.secret
secrets.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
