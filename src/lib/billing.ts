// Single switch for the fiat (Paddle) billing surface. While monetization moves
// to the $SCRP token, the paywall + pricing/upgrade UI are hidden and everyone
// gets full access. Flip this back to `true` to restore tiered plans, the
// pricing page links and the subscription paywall — nothing was deleted.
export const BILLING_ENABLED = false;
